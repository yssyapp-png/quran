# هيكلة تطبيق "القرآن الكريم" — مصحف المدينة المنورة (رواية حفص عن عاصم)

## 1. التقنية المقترحة

**Flutter** هو الخيار الأفضل هنا، للأسباب التالية:
- كود واحد لـ iOS وAndroid مع أداء قريب من الأصلي (Native)، مهم جدًا لأن رسم صفحات المصحف والتمرير السلس (60fps) حساس للأداء.
- دعم ممتاز للخطوط العربية المخصصة (خط HafsSmart/QCF) ورسم النصوص المعقدة (Text Shaping) عبر محرك Skia.
- مكتبات ناضجة لكل الاحتياجات: `just_audio` (صوت + قوائم تشغيل)، `photo_view`/`InteractiveViewer` (تكبير/تصغير)، `drift` أو `sqflite` (قاعدة بيانات محلية)، `flutter_local_notifications` (تنبيهات).
- هذا يبني على المشروع الحالي (quran) بدون إعادة كتابة من الصفر.

البديل: Native (Swift + Kotlin منفصلين) يعطي أفضل أداء ممكن لكنه يضاعف وقت التطوير والصيانة — غير مبرر إلا لو الفريق كبير ولديه فريقين منفصلين.

## 2. تقسيم المشروع إلى وحدات (Modules)

```
lib/
  core/
    theme/            # AppColors, ThemeData (ليلي/نهاري), ThemeController  ✅ موجود
    constants/        # أسماء السور، ثوابت الصوت، روابط القراء
    utils/            # تنسيق الأرقام العربية، تحويلات الصفحات↔الأجزاء↔الأحزاب

  data/
    local/
      quran_page_repository.dart   # يبني صفحات المصحف من hafs_smart_v8.json (line_start/line_end) ✅ الأساس موجود
      database/                    # drift/sqflite: bookmarks, notes, settings, reading_progress
    remote/
      tafsir_api.dart               # تفسير الميسر (alquran.cloud)  ✅ موجود كخدمة
      reciters_api.dart             # قائمة القراء وروابط الصوت لكل رواية

  features/
    splash/                # شاشة البداية المتحركة
    onboarding/            # شرح مختصر لأول مرة
    home/                  # الرئيسية: آخر قراءة، بحث سريع، اختصارات (فهرس/تفسير)
    mushaf/                # ⭐ العنصر الأساسي: صفحة المصحف (PageView + تكبير + قراءة مريحة)
    index/                 # الفهرس الشجري: سور / أجزاء / أرباع / أحزاب
    tafsir_sheet/          # نافذة التفسير السريع (Bottom Sheet)
    audio_player/          # المشغل المتقدم + تتبع الآية (Karaoke) + التكرار
    search/                # بحث لحظي مع تمييز النتائج
    bookmarks/             # العلامات المرجعية وآخر موضع توقف
    reminders/             # تنبيهات الورد اليومي / الكهف / الملك

  widgets/               # عناصر مشتركة (أزرار، مؤشرات، شرائط تمرير)
  main.dart
```

هذا التقسيم يوسّع الهيكل الحالي (`models/services/screens/widgets`) إلى بنية Feature-first أنسب لمشروع بهذا الحجم، مع فصل واضح بين `data` (مصادر البيانات) و`features` (الشاشات والمنطق الخاص بكل ميزة).

## 3. أهم نقطة تقنية: صفحة المصحف المطابقة لمصحف المدينة

بيانات `hafs_smart_v8.json` التي دمجناها فعلاً تحتوي على `page` و`line_start` و`line_end` لكل آية — هذا بالضبط ما يلزم لإعادة بناء **تخطيط كل صفحة سطرًا بسطر** كما في المصحف المطبوع، بدون الحاجة لصور. النهج المقترح:
1. تجميع الآيات حسب رقم الصفحة (604 صفحة).
2. رسم كل صفحة كسلسلة أسطر (15 سطرًا عادةً) باستخدام خط HafsSmart، مع محاذاة كل سطر حسب `line_start/line_end`.
3. عرض الصفحات داخل `PageView` أفقي (يمين → يسار) مع `InteractiveViewer` لكل صفحة للتكبير/التصغير دون فقدان الوضوح (لأنه نص Vector عبر خط، وليس صورة).

هذا أفضل من الاعتماد على صور صفحات (أثقل حجمًا، ولا تدعم تمييز آية أثناء التلاوة أو البحث داخل الصفحة).

## 4. هيكل قاعدة البيانات المحلية (drift/sqflite)

```sql
-- آخر موضع قراءة (سجل واحد يُحدَّث باستمرار)
last_read (sura_no INT, aya_no INT, page INT, updated_at DATETIME)

-- العلامات المرجعية (يمكن أن تتكرر)
bookmarks (id INTEGER PRIMARY KEY, sura_no INT, aya_no INT, page INT,
           label TEXT NULL, created_at DATETIME)

-- ملاحظات المستخدم على آية معينة (اختياري لمرحلة لاحقة)
notes (id INTEGER PRIMARY KEY, sura_no INT, aya_no INT, note TEXT, created_at DATETIME)

-- التفضيلات العامة
settings (key TEXT PRIMARY KEY, value TEXT)
-- أمثلة مفاتيح: theme_mode, selected_reciter, font_scale, repeat_count, daily_reminder_enabled

-- تقدّم الورد اليومي (لتتبع الإنجاز والتنبيهات)
daily_progress (date TEXT PRIMARY KEY, pages_read INT, kahf_read BOOL, mulk_read BOOL)
```

ملاحظة: العلامات المرجعية والتفضيلات الحالية مبنية حاليًا على `shared_preferences` (بسيطة وكافية للمرحلة الحالية). عند إضافة الملاحظات وتتبع الورد اليومي، الأفضل الانتقال إلى **drift** (طبقة فوق sqflite بأمان أنواع Type-safe) لأن البيانات ستصبح علائقية.

## 5. تدفّق الشاشات (UI Flow)

```
Splash (شعار + آية متحركة، 2-3 ثوانٍ)
  → Onboarding (مرة واحدة فقط لأول تشغيل، قابلة للتخطي)
    → Home
        ├─ بطاقة "متابعة القراءة" → Mushaf (يفتح على آخر صفحة)
        ├─ شريط بحث لحظي → Search Results → Mushaf (ينتقل للآية مع تمييزها)
        ├─ زر الفهرس → Index (تبويبات: سور / أجزاء / أرباع / أحزاب) → Mushaf
        └─ زر الإعدادات → Settings (القارئ، المظهر، التنبيهات)

Mushaf (الشاشة المحورية)
  ├─ تمرير أفقي بين الصفحات (604 صفحة)
  ├─ لمسة واحدة على الصفحة → إخفاء/إظهار الأشرطة العلوية والسفلية (وضع القراءة المريحة)
  ├─ نقر مزدوج على آية → Bottom Sheet التفسير السريع
  ├─ ضغط مطوّل على آية → قائمة سياقية (حفظ علامة / تكرار / نسخ / مشاركة)
  ├─ زر تشغيل عائم → Audio Player Bar (سفلي، قابل للتوسيع)
  │     يبرز الآية الحالية أثناء التلاوة (Karaoke)، مع خيار التكرار
  └─ زر تكبير الفهرس/الإعدادات كأزرار سريعة في الشريط العلوي المخفي
```

## 6. خطة تنفيذ مقترحة (مراحل)

1. **الأساس (منجز جزئيًا)**: بيانات المصحف محليًا، قراءة سورة-سورة، صوت، تفسير، وضع ليلي/نهاري، علامات مرجعية بسيطة. ✅
2. **صفحة المصحف الحقيقية**: إعادة بناء العرض من "قائمة آيات" إلى "صفحات بتخطيط سطري" + التكبير/التصغير + وضع القراءة المريحة.
3. **الفهرس الشجري**: سور/أجزاء/أرباع/أحزاب مع بحث سريع داخل الفهرس.
4. **البحث اللحظي**: بحث بالنص الإملائي مع تمييز النتيجة والانتقال المباشر.
5. **المشغل المتقدم**: تعدد القراء، تتبع الآية أثناء التلاوة (يتطلب بيانات توقيت لكل آية من كل قارئ)، والتكرار.
6. **التنبيهات الذكية**: الورد اليومي، تذكير الكهف (يوم الجمعة)، وتذكير الملك (كل ليلة).
7. **الهوية البصرية**: Splash متحرك + Onboarding.

## 7. ملاحظة مهمة بخصوص "تتبع الآية أثناء التلاوة (Karaoke)"
هذه الميزة تحتاج **بيانات توقيت (Timestamps)** لكل آية داخل كل مقطع صوتي — أي مصدر صوتي يوفرها (مثل مشاريع مفتوحة مثل "quran-align" أو ملفات توقيت من mp3quran.net لبعض القراء). هذا عنصر يجب حسمه قبل البدء بالتنفيذ لأنه يحدد مصدر الصوت المستخدم (لازم يكون له توقيتات جاهزة، أو نبنيها يدويًا وهذا مكلف جدًا زمنيًا).
